const should = require('should');
const io = require('socket.io-client');

const socketUrl = 'http://0.0.0.0:4546';
const options = {
  transports: ['websocket'],
  'force new connection': true
};

describe('SOCKET', () => {
  xit('TODO');
});
